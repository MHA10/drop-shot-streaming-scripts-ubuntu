# ignoring all build modules
**/dist/
# ignoring all node js modules
**/node_modules/
# ignore public data with images
/public/

.env

.claude

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
